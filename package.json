{
  "name": "agent-tasks",
  "version": "1.0.0",
  "description": "File-based task tracker for parallel pi agents. Tasks are plain markdown in tasks/{open,claimed,done}/; claiming is an atomic rename.",
  "type": "module",
  "keywords": ["pi-package", "tasks", "project-management", "agent-tools"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
